# CDK tsconfig path
CDK_TSCONFIG=tsconfig.cdk.json

# CDK debug switch. Adds '--inspect-brk', if set to true.
CDK_DEBUG=false

# CDK debug host
CDK_DEBUG_HOST=127.0.0.1

# CDK debug port
CDK_DEBUG_PORT=9229
<% environments.forEach((environment) => { %>

# <%= environment %> environment

# TODO set values and uncomment
# CDK_<%= environment.toUpperCase() %>_ACCOUNT=123456789012
# CDK_<%= environment.toUpperCase() %>_REGION=us-east-1
<% }); %>
