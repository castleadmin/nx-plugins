{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "title": "serverless-cdk event executor",
  "description": "Wrapper around the 'sam local generate-event' command. (https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/sam-cli-command-reference-sam-local-generate-event.html)",
  "type": "object",
  "properties": {
    "args": {
      "type": "string",
      "description": "Used to provide additional arguments to the SAM CLI. (https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/sam-cli-command-reference-sam-local-generate-event.html)"
    },
    "eventsDirectory": {
      "type": "string",
      "description": "The path of the directory where the example events are stored."
    },
    "save": {
      "type": "string",
      "description": "Defines the path where the event file should be saved. The path is relative to the events directory."
    },
    "shell": {
      "type": "string",
      "description": "Shell to execute the command with. Uses '/bin/sh' on Unix, and process.env.ComSpec on Windows, if no shell is specified."
    }
  },
  "required": ["eventsDirectory"]
}
