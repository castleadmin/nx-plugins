describe('<%= fileName %>', () => {
  describe('Given a service,', () => {
    test('should return a message if called via HTTP or AWS SDK.', async () => {
      const result = 'answer from service';

      expect(result).toBe('answer from service');
    });
  });
});
