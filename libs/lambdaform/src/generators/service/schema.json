{
  "$schema": "http://json-schema.org/schema",
  "$id": "Service",
  "title": "Lambdaform Service Generator",
  "type": "object",
  "properties": {
    "serviceName": {
      "type": "string",
      "description": "Define service name.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What service name would you like to use?"
    },
    "skipFormat": {
      "description": "Skip formatting files.",
      "type": "boolean",
      "default": false
    }
  },
  "required": ["serviceName"],
  "additionalProperties": false
}
